# Secrets: never committed. secret.key is server-generated at runtime;
# sms.key holds the Textbelt API key and is placed on the server by hand.
php/secret.key
php/sms.key

# Runtime data (leads, analytics, rate limits, logs); keep only the .htaccess guard
data/*
!data/.htaccess
php/smtp.key
